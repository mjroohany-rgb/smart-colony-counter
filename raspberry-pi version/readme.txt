smart-colony-counter originally was a standalone device.
it was created using raspberry Pi board - camera and touch screen.

so, raspberry Pi version code with raspberry Pi camera eanbled feature is Here.

also that device's images are added just for remembering it's Memory.

Notice about openCV: usually needed to compile it frome source code. because lack of ARM version.